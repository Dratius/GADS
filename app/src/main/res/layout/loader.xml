<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#222222"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/supporters"
        android:layout_width="333.4dp"
        android:layout_height="82.67dp"
        android:contentDescription="@string/supporter"
        android:scaleType="centerInside"
        android:src="@drawable/gadds"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/loader"
        android:layout_width="411dp"
        android:layout_height="84dp"
        android:contentDescription="@string/loader"
        app:layout_constraintBottom_toTopOf="@+id/supporters"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/banner" />

    <ImageView
        android:id="@+id/banner"
        android:layout_width="244.02dp"
        android:layout_height="54dp"
        android:contentDescription="@string/banner"
        app:layout_constraintBottom_toBottomOf="@+id/logo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/logo"
        app:srcCompat="@drawable/gadgs" />

    <ImageView
        android:id="@+id/logo"
        android:layout_width="414dp"
        android:layout_height="227.25dp"
        android:layout_marginTop="44dp"
        android:alpha="0.02"
        android:contentDescription="@string/logo"
        android:rotation="31"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.748"
        app:layout_constraintStart_toEndOf="@+id/banner"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:srcCompat="@drawable/gadms" />

</androidx.constraintlayout.widget.ConstraintLayout>